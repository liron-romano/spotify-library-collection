name = "mu-owned-sync"
main = "worker.js"
compatibility_date = "2024-01-01"

# Created via `wrangler kv namespace create OWNED_KV` — paste the resulting id below.
kv_namespaces = [
  { binding = "OWNED_KV", id = "REPLACE_WITH_YOUR_KV_NAMESPACE_ID" }
]
